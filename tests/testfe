<?
$foo = array(7,"foo",3,array(1,2),4);

for ($i=0; $i<3; $i++):

foreach($a, $foo ) {
	if (gettype($a)=="array") {
		print "Array:\n";
		foreach($b, $a) {
			print "\t$b\n";
		}
		print "End of array.\n";
	} else {
		print "$a\n";
	}
//	print "$a\n";
}
endfor;

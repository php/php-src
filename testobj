<?

class foobar {
	function foobar() {
		print "foobar!\n";
		$this->initialized = 1;
	}
};

$foo = new foobar; // or die("Unable to construct foobar\n");
//$word = new COm("word.application");
//$word->visible = true;
//sleep(5);
//$word->quit();
